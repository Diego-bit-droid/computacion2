# El monitor no tiene dependencias externas obligatorias: la TUI usa curses,
# que es parte de la biblioteca estándar de Python en Linux.
# Se deja este archivo (vacío a propósito) para respetar la estructura
# sugerida del TP y para que `pip install -r requirements.txt` no falle.
